[project]
name = "ex1"
version = "0.1.0"
description = "learn the difference betweenn poetry and pip"
authors = [
    {name = "stcozaci"}
]
requires-python = "^3.10"
dependencies = [
    "pandas (<3.0)",
    "requests (>=2.32.5,<3.0.0)",
    "matplotlib (>=3.10.8,<4.0.0)",
]


[tool.poetry]
package-mode = false


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
